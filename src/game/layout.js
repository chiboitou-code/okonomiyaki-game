// お好み焼き本体の表示位置・サイズを、調理フェーズとトッピングフェーズで共通化するための設定。
// ここの数値を変えれば、両方のフェーズに同時に反映される。

export const BODY_WIDTH_RATIO = 0.7; // 画面横幅に対する本体の直径の割合
export const BODY_CENTER_Y_RATIO = 0.55; // 画面縦幅に対する本体中心のY位置の割合
